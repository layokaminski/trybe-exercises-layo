const uppercase = string => string.toUpperCase();

const oneLetter = string => string[0];

const concatStrings = (stringOne, stringTwo) => `${stringOne} ${stringTwo}`;

module.exports = { uppercase, oneLetter, concatStrings }